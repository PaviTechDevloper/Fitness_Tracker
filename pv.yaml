apiVersion: v1
kind: PersistentVolume
metadata:
  name: fitness-pv
spec:
  capacity:
    storage: 10Gi
    accessModes:
      - ReadWriteMany
    persistentVolumeRecliamPolicy: Retain
    nfs:
      path: /var/nfs/general
      server: 10.0.2.222
